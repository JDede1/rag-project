version: "3.9"

services:
  rag-backend:
    build: .
    container_name: rag-backend
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    volumes:
      # Optional: mount logs to host for easy access
      - ./logs:/app/logs
